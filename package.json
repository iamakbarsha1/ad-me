{
  "name": "ad-me",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "db:generate": "pnpm --filter @ad-me/api db:generate",
    "db:migrate": "pnpm --filter @ad-me/api db:migrate",
    "db:push": "pnpm --filter @ad-me/api db:push"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  }
}
